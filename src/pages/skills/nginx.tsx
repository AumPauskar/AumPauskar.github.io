// src/pages/skills/nginx.tsx

export function NginxSkillPage() {
  return (
    <div>
      <h1 className="text-3xl font-bold">Nginx</h1>
      <p className="mt-4 text-lg text-muted-foreground">
        I have used Nginx as a high-performance web server and reverse proxy. I have experience configuring it to serve static content, proxy requests to back-end services, and set up SSL/TLS.
      </p>
    </div>
  );
}